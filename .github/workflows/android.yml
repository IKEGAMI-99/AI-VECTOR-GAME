name: Android CI + Release

on:
  push:
    branches: [ main, feat/timed-rounds-v0.7 ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Apply app version
        shell: bash
        run: |
          sed -i 's/versionCode = [0-9][0-9]*/versionCode = 8/' app/build.gradle.kts
          sed -i 's/versionName = "[^"]*"/versionName = "0.7.0"/' app/build.gradle.kts

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android components
        shell: bash
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager \
            "platforms;android-35" \
            "build-tools;35.0.0" \
            "ndk;29.0.14206865" \
            "cmake;3.31.6"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Android lint
        run: gradle :app:lintDebug --stacktrace --no-daemon

      - name: Build debug and release APKs
        run: gradle :app:assembleDebug :app:assembleRelease --stacktrace --no-daemon

      - name: Verify release APK structure and signature
        shell: bash
        run: |
          set -euo pipefail
          APK="app/build/outputs/apk/release/app-release.apk"
          test -s "$APK"
          unzip -Z1 "$APK" > /tmp/apk-files.txt
          for REQUIRED in "AndroidManifest.xml" "classes.dex" "resources.arsc" "lib/arm64-v8a/libai_vector_native.so"; do
            grep -Fxq "$REQUIRED" /tmp/apk-files.txt || { echo "::error::Required APK entry missing: $REQUIRED"; exit 1; }
          done
          rm -rf /tmp/apk-check && mkdir -p /tmp/apk-check
          unzip -q "$APK" "lib/arm64-v8a/libai_vector_native.so" -d /tmp/apk-check
          readelf -Ws /tmp/apk-check/lib/arm64-v8a/libai_vector_native.so > /tmp/native-symbols.txt
          grep -q "Java_com_aivectorgame_app_ai_NativeEngine_nativePredictTopTokens" /tmp/native-symbols.txt
          grep -q "Java_com_aivectorgame_app_ai_NativeEngine_nativeScoreContinuations" /tmp/native-symbols.txt
          grep -q "Java_com_aivectorgame_app_ai_NativeEngine_nativeEmbedding" /tmp/native-symbols.txt
          "${ANDROID_HOME}/build-tools/35.0.0/apksigner" verify --verbose --print-certs "$APK"
          sha256sum "$APK"

      - name: Read app version
        id: version
        shell: bash
        run: |
          VERSION=$(sed -n 's/.*versionName = "\([^"]*\)".*/\1/p' app/build.gradle.kts | head -1)
          test -n "$VERSION"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=v$VERSION" >> "$GITHUB_OUTPUT"

      - name: Prepare named APK
        shell: bash
        run: |
          cp app/build/outputs/apk/release/app-release.apk "AI-VECTOR-GAME-v${{ steps.version.outputs.version }}.apk"

      - name: Upload release APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: ai-vector-game-v${{ steps.version.outputs.version }}-release
          path: AI-VECTOR-GAME-v${{ steps.version.outputs.version }}.apk
          if-no-files-found: error
          retention-days: 30

      - name: Publish or refresh GitHub Release
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          TAG="${{ steps.version.outputs.tag }}"
          APK="AI-VECTOR-GAME-v${{ steps.version.outputs.version }}.apk"
          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" "$APK" --clobber
          else
            gh release create "$TAG" "$APK" --title "AI VECTOR GAME $TAG" --generate-notes
          fi
